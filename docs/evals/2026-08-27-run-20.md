# Eval run 20 — billed `feature` + `feature-adaptive` pin — 2026-08-27

**Trigger:** [2.2.0 plan](../plans/2026-08-24-guild-v2-adaptive-graph.md) Task 12. [Design](../plans/2026-08-24-guild-v2-adaptive-graph-design.md). Tasks 6–11 already on `feat/v2.2.0-graph`. This is the billed pin of `coordinator_hash` after default `graph.md`.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature feature-adaptive
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompts `/make-feature Tag --api` then `/make-feature Tag --api --adaptive`. Run id `20260827T092745Z`. `EVAL_JUDGE` not set.

**Score:** **0/2 cases.** Both timed out at 1203s.

| Case | Verdict | Graph | Close | Notes |
| --- | --- | --- | --- | --- |
| `feature` | **11/13 FAIL** | **FAIL** — `graph.md` missing `PARALLEL:` | **PASS** | Prose graph at `docs/delivery/tag-api/graph.md` (`NODES:` / `EDGES:` headings, no `PARALLEL:` / `ON-FAIL:`). Token ceiling REGRESSED. |
| `feature-adaptive` | **13/18 FAIL** | **PASS** — stub labels | **PASS** | Packet PASS (`TO:` is a node). `peer-router.md` found 0. Handoff FAIL. Harvest FAIL. |

The 2.2.0 ship gate is **default `feature` graph + close**. Graph missed. Do not bump VERSION. Do not raise `$8.50` / 1200s / 14.5M. Do not loosen `check_delivery_graph_file` or `check_delivery_close_file`. Do not uncomment `check_subagent_log`.

## `feature` (`/make-feature Tag --api`)

Workdir kept: `laravel-agents-eval-feature.XXXXXX.DmgzRuJ51q`. Delivery dir `docs/delivery/tag-api/`.

**Graph FAIL.** File exists. Helper required line prefixes `NODES:` / `EDGES:` / `PARALLEL:` / `ON-FAIL:`. The coordinator wrote a markdown briefing:

```
NODES:
- laravel-team:database-developer — owns …
EDGES:
- database-developer → backend-developer
SKIPPED:
- frontend-developer — `--api` flag
```

`PARALLEL:` and `ON-FAIL:` are absent. Copy-and-fill of `skills/delivery-templates/graph.md` did not happen. Do not widen the helper to accept headings-plus-bullets.

**Close PASS.** `docs/delivery/tag-api/close.md` helper shape: `VERIFIED:` / `NOT-CHECKED:` / `STATUS: running` / `BOARD:`. `VERIFIED:` starts the line — not `VERIFIED (`.

Other rows: Tag artifacts, feature test, delegation (4 agents: database-developer, backend-developer, qa-engineer, tech-lead), `done when:`, harvest, stage returns — PASS. `$LOG` `NOT-CHECKED` FAIL (close file still has the label).

**Cost:** billed **$6.93** — **within** $8.50. Rate table **STALE** (`agrees: false`; implied cache-write multiplier 0.69). Attributed **17,161,133** tokens — **REGRESSED** vs 14,500,000. Do not raise the token ceiling. Attributed: main opus-5 $6.00 (46 tools); backend-developer sonnet-5 $1.73 (61); qa-engineer sonnet-5 $1.65 (33); tech-lead opus-4-8 $1.55 (20). Wall 1203s within 1900s.

## `feature-adaptive` (`/make-feature Tag --api --adaptive`)

Workdir kept: `laravel-agents-eval-feature-adaptive.XXXXXX.k8exFJBImL`. Delivery dir `docs/delivery/tag/`.

**Graph PASS.** Stub-shaped:

```
NODES: database-developer, backend-developer, qa-engineer, tech-lead, security-engineer, peer-router
EDGES: database-developer -> backend-developer, backend-developer -> qa-engineer, backend-developer -> tech-lead, backend-developer -> security-engineer
PARALLEL: qa-engineer + tech-lead
ON-FAIL: stop
```

**Packet PASS.** `docs/delivery/tag/packets/backend-developer-to-security-engineer.md` — `FROM:` / `TO:` / `SUMMARY:` / `PATHS:`. `TO: security-engineer` is a `NODES:` token. Packet body says the coordinator Wrote the fallback (`backend-developer named security-engineer in NEXT but wrote no packet itself`). Writer-filed hop was not needed.

**Hop persist FAIL.** `check_adaptive_peer_router` found 0 `stages/peer-router.md`. Cost ledger still shows a `peer-router` spawn (sonnet-5, $0.19, 7 turns, Read+Bash). `$FULL_LOG`: first packet rejected (`WHY:` vs `SUMMARY:`); coordinator fixed labels; "Packet validated. Spawning the adaptive hop." `security-engineer` was Agent-spawned ($0.96, 17 turns). The coordinator did not persist the read-only `peer-router.md` stage file. Do not drop the six-label file check.

**Handoff FAIL.** No `handoff` on `$FULL_LOG` or `close.md`. The live board used "Spawning the adaptive hop" instead of a `handoff:` line.

**Close PASS.** `docs/delivery/tag/close.md` helper shape. Last Write still looks like the post-plan seed (`VERIFIED:` is `composer install`; BOARD still lists four stages queued). Later overwrites did not land. Labels-first still holds. Do not loosen the helper.

**Other Adaptive misses (not the 2.2 graph gate):** harvest `stack.md` / `log.md` FAIL. `backend-developer.md` missing `FLAGS:` — the line is `FLAGS (carried forward, unchanged, pending security-engineer sign-off):` (parenthetical after the label). Do not accept `FLAGS (` as `FLAGS:`.

**Cost:** no `result` line (timeout), so `billed.usd` is null. Attributed **$12.65** / **13,338,053** tokens — **within** 1900s (1203s), **within** 14,500,000 tokens. No billed-dollar line to compare to $8.50. Attributed: main opus-5 $5.33 (45 tools, 6 Agent); backend-developer sonnet-5 $1.91 (49); database-developer sonnet-5 $1.93 (37); security-engineer opus-5 $0.96 (17); plus qa-engineer, tech-lead, peer-router. Do not raise ceilings.

**FLAG (unchanged from run 19):** `agents/peer-router.md` still says spawn only when **a writer has named** a packet. This run spawned the router on a **coordinator fallback** packet, so that sentence did not block the hop. The miss is persist + handoff line, not a skip of the router. Do not loosen helpers to hide it.

## Hash pin

`coordinator_hash` is `70769b13d9a54ed83bf7667ab011421ca21fa37dd893bc9f40b6faa8d5f53c24`, `as_of` 2026-08-27, `waivers: []`. The pin records that this billed run exercised default `graph.md` and `--adaptive` on-graph. Default graph **FAIL** (missing `PARALLEL:`). Adaptive graph labels **PASS**. Close **PASS** on both. Adaptive hop persist/handoff **FAIL**. The 2.2.0 gate is default graph + close PASS, not 11/13 or 13/18.

**What this closes:** the billed 2.2.0 measurement. Inventory hash pin is current.

**What this leaves open:** Guild v2 graph (2.2.0). VERSION stays **2.1.1**. Unreleased stays Unreleased. Do not tag 2.2.0. Do not raise ceilings.
