# Eval run 9 — 1.42.0 orchestration-contract fix, billed `feature` re-pin — 2026-08-18

**Trigger:** the orchestration-contract-fix plan (`docs/superpowers/plans/2026-08-18-orchestration-contract-fix.md`, Task 6) shipped the Interface-block edit (build/patch refusal + verify-before-advancing) and the coordinator routing-table / Working-interface note. Inventory was expected red until a billed `feature` run re-pinned `coordinator_hash`. This is that run.

**Score:** 10/10 PASS. Every answer-key check held: tags migration, `Tag` model, tag route, feature test, delegation (4 distinct specialists), `done when:` on the board, closing-stream `VERIFIED` / `NOT-CHECKED`, and both harvest artifacts. Harvest is not a regression of v1.41.0.

**Cost:** $9.00 billed, 1203s (harness printed `TIMED OUT after 1203s` then still scored the artifacts), run id `20260818T191133Z`. Soft ceilings: duration within 1900s; **REGRESSED** vs the $8.50 cost ceiling and the 14,500,000-token ceiling (19,725,180 attributed). Attributed agents: main (opus-5, $7.47), backend-developer (sonnet-5, $3.69), database-developer (sonnet-5, $0.70), qa-engineer (sonnet-5, $0.59), plus tech-lead (opus-4-8) in the cost ledger. Artifacts are local-only and gitignored — this write-up is the durable record.

**What the new Interface clause looked like:** the main-thread stream (`feature.full-text.log`) did not edit app code. Attributed main-thread tools were `Agent` ×4, `Bash` ×36, `Write` ×2, `SendMessage` ×4, `ToolSearch` ×1 — no `Edit`. The two Writes are the harvest files under `docs/**`. When stage (b) shipped a Pest-green backend that was wrong on the real HTTP kernel (authenticated write → 419; guest write → 500 because `redirectGuestsTo(route('login'))` and this fixture has no `login` route), the orchestrator wrote "Re-briefing the same agent with the gap named, rather than patching it myself." Tech-lead's two actionable findings were treated the same way: "Routing both fixes to their owner rather than patching myself." Verify-before-advancing is visible on the board itself: `✔ a` is annotated "I re-ran migrate/rollback + db:table", and the turn immediately before that mark is "Verifying stage (a) myself rather than trusting the claim." `✔ b¹` is annotated "I verified 4 request shapes." A specialist's `STATUS: done` was not treated as a `✔`.

**Harvest evidence** (read on the kept workdir, not by the check's word):

- `docs/team/stack.md` — "Harvested 2026-08-18 during `/make-feature Tag --api`. **CLAUDE.md describes an aspirational stack this repo does not have** — the table below is what actually exists."
- `docs/delivery/tag/log.md` — "**Coordinator verification found what the agent's own tests could not.** The agent verified via Pest only. Pest auto-bypasses CSRF (`runningUnitTests()`), and JSON-only assertions hide `Accept`-dependent behaviour."

**What this leaves open:** the run hit `EVAL_TIMEOUT=1200` (1203s on the clock) before a coordinator closing answer. `$LOG` (eval-cost `--text-only`, falling back to concatenated assistant text when there is no `result` line) is what `check_log` greps; that fallback is how `VERIFIED` / `NOT-CHECKED` passed — they appear as the tech-lead return quoted into the reconstructed log, not as the orchestrator's own final six-field close. `$FULL_LOG` (main-thread turns only) ends mid-board at `Wai`. That is the same observability gap [run 8](2026-08-12-run-8.md) already filed: this harness cannot see per-stage specialist returns, and on a timeout it also cannot see a coordinator close that never got written. Do not re-add `check_stage_return_shape`. The cost/token regressions vs the 2026-08-04 `feature` seed are recorded, not absorbed — three fix rounds on the real 500 plus the timeout are the shape, not a reason to raise the ceiling in this task.

**Hash pin:** `coordinator_hash` in `tests/eval/baseline.json` is now `a4c1bce301aef50337c3002bb29ee0d5653c2504175239d36511fdffc22e224f`, `as_of` 2026-08-18, waivers empty.
