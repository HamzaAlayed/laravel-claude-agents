# Eval run 15 — close.md stub copy, billed `feature` — 2026-08-21

**Trigger:** [close.md stub-copy plan](../plans/2026-08-21-guild-v2-close-stub.md) Task 6. Canonical stubs + copy-then-fill already on `feat/close-md-stub`. This is the billed `feature` re-pin of `coordinator_hash` after that slice.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260821T125708Z`.

**Score:** **9/12 FAIL.** Harness printed `TIMED OUT after 1202s`. Three checks missed:

- `final answer carries VERIFIED` — `$LOG` is 178 bytes of mid-replan prose (“Stage 4 is running…”). No `VERIFIED`.
- `final answer carries NOT-CHECKED` — same `$LOG`.
- `close file missing VERIFIED: (tag/close.md)`

Harvest, delegation (4 agents), `done when:`, Tag artifacts, and basename check all PASS. No `-fixes`. `check_stage_return_files` PASS — including coordinator-persisted `tech-lead.md` with line-start `VERIFIED:` (unlike run 14). Do not uncomment `check_subagent_log`.

**Cost:** **$7.60 billed** / 15,482,003 attributed tokens. Soft ceilings: **within** 1900s (1202s), **within** $8.50, **REGRESSED** vs 14,500,000 tokens. Attributed: main opus-5 $5.76 (36 tools, 6 Agent), backend-developer sonnet-5 $1.81 (49), database-developer sonnet-5 $1.77 (49), qa-engineer sonnet-5 $1.38 (24), tech-lead opus-4-8 $2.29 (read-only; not in the printed four-row ledger). Do not raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling.

**Close file (the 2.0 gate):** `docs/delivery/tag/close.md` exists (122 lines, 7159 bytes). It is a **journal**, not a four-line stub copy. First lines are `# Close file — Tag (--api)` then `STATUS: planning complete, stage 1 dispatched` then `BOARD` (no colon). Labels are `VERIFIED (coordinator, at plan time)` / `NOT-CHECKED (at plan time)` — parenthesis, no colon, so `^VERIFIED:` misses. Last section is the stage-3 re-plan; stage 4 was still running when the timeout killed the process. `STATUS: running|done|stopped` never appears. The stub files were not byte-copied. Do not loosen the helper to accept `VERIFIED (`.

**What moved vs [run 14](2026-08-21-run-14.md):** timeout again (run 14 finished in 1046s). Billed USD still inside $8.50. `tech-lead.md` now has the six labels at line start. Close file grew into a sectioned log instead of staying four lines. Spawn cap was re-planned from `3 stages · cap 5` to `5 stages · cap 8` (6 Agent calls; cap not hit). Token volume crossed the 14.5M soft ceiling; do not raise it.

**Hash pin:** `coordinator_hash` is `4dd22de19c8c7f592df075532a4b09911e7ee32f32ba9928f787cd8fdbddca03`, `as_of` 2026-08-21, waivers empty. The pin records that this billed run exercised the stub-copy slice. It does **not** claim 12/12 or a 2.0.0 ship.

**What this leaves open:** a timed-out run still fails the close-file helper on `VERIFIED (`, and the first Write was not a stub copy. VERSION stays **1.45.0**.
