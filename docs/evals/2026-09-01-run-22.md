# Eval run 22 — billed `feature-adaptive` pin — 2026-09-01

**Trigger:** [2.2.1 plan](../plans/2026-08-31-guild-v2-adaptive-persist.md) Task 6. [Design](../plans/2026-08-31-guild-v2-adaptive-persist-design.md). Tasks 1–5 already on `feat/v2.2.1-adaptive-persist`. This is the billed `feature-adaptive` pin of `coordinator_hash` after Adaptive hop persist + packet-exists FLAG.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature-adaptive
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api --adaptive`. Run id `20260901T084147Z`. `EVAL_JUDGE` not set.

**Score:** **16/18 FAIL.** Harness printed `TIMED OUT after 1203s`. Adaptive-specific rows and close all PASS:

- `check_adaptive_packet` — PASS. `docs/delivery/tag/packets/database-developer-to-backend-developer.md` carries `FROM:` / `TO:` / `SUMMARY:` / `PATHS:`. `TO: backend-developer` is a `NODES:` token.
- `check_adaptive_peer_router` — PASS. `docs/delivery/tag/stages/peer-router.md` exists with six labels (`STATUS:` / `DID:` / `VERIFIED:` / `NOT-CHECKED:` / `FLAGS:` / `NEXT:`). `VERIFIED: valid`.
- `check_adaptive_handoff` — PASS. Board line on `$FULL_LOG`: `handoff: database-developer → backend-developer`.
- `check_delivery_close_file` — PASS.

The two misses are `$LOG` `VERIFIED` / `NOT-CHECKED` — timeout killed the run before a closing answer. Close file still has both labels. Harvest PASS (`docs/team/stack.md`, `docs/delivery/tag/log.md`). Graph PASS (stub labels, registered nodes). Delegation (5 agents: database-developer, peer-router, backend-developer, qa-engineer, tech-lead), `done when:`, Tag artifacts, feature test — all PASS. Do not uncomment `check_subagent_log`. Do not require 18/18. Do not loosen helpers to accept “Spawning the adaptive hop”.

**Writer-filed, not coordinator fallback.** `log.md` Adaptive hop: "Packet written by the writer itself (not a coordinator fallback)." Pass 1 valid. Coordinator persisted `peer-router.md` then printed `handoff:` then Agent-ed `backend-developer`. Fallback was not needed.

**`peer-router` spawn: yes.** Five Agent launches. peer-router sonnet-5 $0.12 / 3 turns (Read + Bash only). FLAG copy now says spawn when a packet exists (writer or fallback); this run was writer-filed. Do not loosen the helper.

**Cost:** billed **$5.44** — **within** $8.50. Rate table **STALE** (`agrees: false`; implied cache-write multiplier 0.91). Attributed **10,158,559** tokens — **within** 14,500,000. Attributed: main opus-5 $5.08 (64 tools, 5 Agent); qa-engineer sonnet-5 $1.13 (32); tech-lead opus-4-8 $1.22 (17); backend-developer sonnet-5 $0.94 (38); database-developer sonnet-5 $0.60 (27); peer-router sonnet-5 $0.12 (3). Wall 1203s within 1900s. Do not raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling.

**Close file (the 2.0 gate, still PASS):** `docs/delivery/tag/close.md` exists. Helper shape: `VERIFIED:` / `NOT-CHECKED:` / `STATUS: running` / `BOARD:`. `VERIFIED:` starts the line — not `VERIFIED (`. Last board has qa-engineer `▶` (timeout during c ‖ d). BOARD on disk names peer-router ✔; handoff line is on `$FULL_LOG`. Do not loosen the helper.

**Basename:** `database-developer.md`, `backend-developer.md`, `peer-router.md`, `tech-lead.md`; no `-fixes`; no `qa-engineer.md` — timeout while that stage was still running. Feature test still landed.

**What moved vs [run 20](2026-08-27-run-20.md):** Adaptive `peer-router.md` and `handoff:` both PASS (run 20 missed both; live board used “Spawning the adaptive hop”). Packet still PASS. Close still PASS. Harvest PASS (run 20 FAIL). Graph still PASS. Writer-filed hop; FLAG copy no longer requires “writer has named.” Tokens 10.2M vs 13.3M attributed. Billed $5.44 vs run 20 billed.usd null. Same timeout class (1203s).

**Hash pin:** `coordinator_hash` is `162bef022577e309611910ff17301072eb01555b9c34cb09f4b000ad054be0b6`, `as_of` 2026-09-01, waivers empty. The pin records that this billed run exercised `--adaptive` hop persist and **passed** packet, `peer-router.md`, handoff, and close. Score is FAIL on the two `$LOG` rows only; the 2.2.1 gate is Adaptive+close PASS, not 16/18.

**What this closes:** Adaptive hop persist (run 20 leftovers 1 and 2). Inventory hash pin is current. VERSION is **2.2.1**.

**What this leaves open:** Token ceiling still 14.5M. Do not tag until CI is green on the PR.
