# Eval run 13 — close.md skeleton stickiness, billed `feature` — 2026-08-20

**Trigger:** [close-file stickiness plan](../plans/2026-08-20-guild-v2-close-stick.md) Task 7. Copy-paste skeleton + STATUS vocab + re-brief path already on `feat/guild-v2`. This is the billed `feature` re-pin of `coordinator_hash` after that slice.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260820T151938Z`.

**Score:** **11/12 FAIL.** Harness printed `TIMED OUT after 1203s`. One file check missed:

- `close file missing VERIFIED: (tag/close.md)`

`check_stage_return_files` **PASS** — four registered basenames only (`database-developer.md`, `backend-developer.md`, `qa-engineer.md`, `tech-lead.md`). No `-fixes` sibling. Harvest, delegation (4 agents), `done when:`, Tag artifacts, and `check_log` `VERIFIED` / `NOT-CHECKED` all PASS. Do not treat the log greps as a coordinator close — `$LOG` is 15,171 bytes of mid-run prose plus specialist returns.

**Cost:** No `result` line (timeout), so `billed.usd` is null. Attributed **$10.73** / 12,049,772 tokens — **above** the $8.50 soft ceiling (the harness did not print a dollar REGRESSED line because billed was empty). Wall clock **within** 1900s (1203s). Tokens **within** 14,500,000. Attributed: main opus-5 $5.04 (31 tools), qa-engineer sonnet-5 $1.77 (33), backend-developer sonnet-5 $1.55 (39), database-developer sonnet-5 $0.85 (31), tech-lead opus-4-8 $1.51 (read-only; not in the printed four-row ledger). Do not raise `max_usd` or `EVAL_TIMEOUT`.

**Close file (the 2.0 gate):** `docs/delivery/tag/close.md` exists. Last write is still mid stage 3 (`STATUS: stages a + b done…; stages c + d running`). Labels are `VERIFIED (coordinator, own hands):` and `NOT-CHECKED (coordinator):` — colon after a parenthetical, so `grep -q 'VERIFIED:'` misses. `STATUS:` is present but the value is a sentence, not `running|done|stopped`; the vocab check would have FAILed next had the colon labels passed. `qa-engineer.md` and `tech-lead.md` exist on disk while `close.md` still says those stages are `▶` — overwrite after every stage still missed. Board header used `4 stages · cap: 6 spawns · done when:` then a re-plan to `7 stages · cap: 9 spawns`. Do not loosen the helper to accept `VERIFIED (`.

**What moved vs [run 12](2026-08-20-run-12.md):** `-fixes` basename is gone. Close file, spawn cap, and harvest still happen. The copy-paste skeleton did not stick.

**Hash pin:** `coordinator_hash` is `bbc48c43caabd28aebc501b928d6124d796f6c42c82b67cbaf71758a5422dcc4`, `as_of` 2026-08-20, waivers empty. The pin records that this billed run exercised the stickiness slice. It does **not** claim 12/12 or a 2.0.0 ship.

**What this leaves open:** `close.md` is written, still not in helper shape, still not overwritten after later stages. VERSION stays **1.45.0**. Do not uncomment `check_subagent_log`.
