# Eval run 10 — 1.43.0 `feature` with `EVAL_TIMEOUT=1900` — 2026-08-19

**Trigger:** [run 9 cost](2026-08-19-run-9-cost.md) finding 1 plus [the follow-up experiment](../plans/2026-08-19-run-9-cost-next.md). One billed `feature` on pack v1.43.0 (`46ed8e5`), same fixture, same Interface, `max_usd` left at $8.50, `EVAL_TIMEOUT=1900`, `KEEP_WORKDIR=1`, `KEEP_TRANSCRIPT=1`. Not a fix. Not a ceiling raise.

**Command:**

```sh
EVAL_TIMEOUT=1900 KEEP_WORKDIR=1 KEEP_TRANSCRIPT=1 ./tests/eval/run-evals.sh feature
```

**Score:** 10/10 PASS, 1387s, **no** `TIMED OUT`. Every answer-key check held, including harvest. Run id `20260819T093031Z`. Claude Code 2.1.235. Prompt `/make-feature Tag --api`.

**Cost:** $6.70 billed (`6.697650…`), 10,785,056 attributed tokens, `coverage_of_billed` 134% (attributed $8.95 / billed $6.70). `rate_table_check.agrees: true`. Soft ceilings: **within** 1900s, **within** $8.50, **within** 14,500,000 tokens. Stream `type: result` `subtype: success` `duration_ms: 1384575` `stop_reason: end_turn`.

Attributed agents (from `feature.cost.json`):

| Agent | Turns | USD | Tools |
| --- | ---: | ---: | --- |
| main (opus-5 / opus-4-8 in the billed ledger) | 66 | $5.09 | `Agent` ×6, `Bash` ×27 |
| backend-developer (sonnet-5) | 60 | $1.70 | `Bash` ×38, `Edit` ×9, `Read` ×6, `Write` ×7 |
| qa-engineer (sonnet-5) | 32 | $0.97 | `Bash` ×26, `Edit` ×3, `Read` ×2, `Write` ×1 |
| tech-lead | 15 | $0.82 | `Bash` ×3, `Read` ×12 |
| database-developer (sonnet-5) | 18 | $0.37 | `Bash` ×18 |

Main-thread tools: **no `SendMessage`, no `Edit`, no `Write`.** Six `Agent` launches (seed and run 9 were four): schema, api, tests∥review, then a planned fix stage and a harden stage after QA found a live HTTP 500.

## Outcome table (filled)

| Fact | This run |
| --- | --- |
| Wall vs kill | 1387s. No `TIMED OUT`. Inside 1900. Past the default 1200, which is why this experiment raised `EVAL_TIMEOUT`. |
| Bill | $6.70 ≤ $8.50. Seed was $6.55 / 1456s. Run 9 was $9.00 / 1203s kill. |
| Loop? | **No `SendMessage` re-brief loop.** An unauthenticated non-JSON hit on `api/v1/tags` did return HTTP 500 (`route('login')` missing). The orchestrator verified it, grew the board 3→5, and dispatched a **new** `backend-developer` stage plus a `qa-engineer` harden stage. CSRF 419 did not appear (writes stayed on the `api` group). Auth for real clients was a **checkpoint** (Sanctum), same shape as the 2026-08-04 seed — not a specialist defect to re-brief. |
| Close? | Orchestrator’s **own** `VERIFIED` / `NOT-CHECKED` on `$FULL_LOG` (`feature.full-text.log`), matching `$LOG`. Not a specialist concatenated into a timeout fallback. |
| Harvest | Read on the kept workdir: `docs/team/stack.md` and `docs/delivery/tag-api/log.md`. |

**Row ticked:** finishes with no 419/500 *re-brief loop* (`SendMessage` not used for HTTP-kernel retries) and bills **≤ $8.50**.

**Reads as:** Finding 1 holds. Ceiling stays. Run 9’s $9.00 was the `SendMessage` ×4 loop plus the 1200s kill, not “1.42’s new happy path.” Do not reseed `max_usd`. Do not add `route('login')` to the fixture as a follow-up — this run already completed without it, via `routes/api.php` + a Sanctum checkpoint + a *new-stage* fix for the guest-500 on the API the run itself added.

A guest-500 that is found, budgeted as a new stage, and owned by `backend-developer` is not the run-9 loop. Run 9’s operational marker was `SendMessage` ×4 on main and “Re-briefing the same agent … rather than patching it myself.” This run’s marker is `Agent` ×6 and “Re-planning.”

## `$SUBAGENT_LOG` (sidecar, not a table row)

`feature.subagent.log` is **0 bytes**. The extractor is not broken: the transcript has 125 assistant turns with top-level `parent_tool_use_id` set (and `subagent_type`), vs 66 main-thread turns. Every nested turn’s `message.content` is **`tool_use` only** — 125 tool_use blocks, 0 text blocks. `subagent_text()` concatenates text; there is none.

Do **not** uncomment `check_subagent_log` in `checks_feature()`. On this transcript those greps would fail a 10/10 PASS. Per-stage `STATUS` / `DID` / `VERIFIED` remains unmeasured in the derived log. Do not re-add `check_stage_return_shape`. Do not grep `stream.jsonl` from `checks_*`.

## What this leaves open

- Pack v1.43.0 is local `main` (`46ed8e5`), not yet tagged or pushed. This run measured that commit.
- Nested specialist *prose* is still invisible to `$SUBAGENT_LOG` on Claude Code 2.1.235 stream-json. A later CLI that emits text on subagent turns can be re-inspected without a new extractor.
- Results dir `tests/eval/results/20260819T093031Z` is gitignored. This write-up is the durable record.
