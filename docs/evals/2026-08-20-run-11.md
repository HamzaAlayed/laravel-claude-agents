# Eval run 11 — 1.45.0 stage-return files, billed `feature` — 2026-08-20

**Trigger:** [L3 stage-return design](../plans/2026-08-19-l3-stage-returns-design.md) Task 7. Prompt + eval helper already on `feat/l3-stage-returns`. This is the billed `feature` re-pin of `coordinator_hash` and the first live assertion of `check_stage_return_files`.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Claude Code 2.1.235. Prompt `/make-feature Tag --api`. Run id `20260820T120724Z`. A `mkdir -p "$WORK/.claude"` landed immediately before this run so the board-feed truncate no longer fails on a missing directory.

**Score:** **10/11 FAIL.** Harness printed `TIMED OUT after 1203s`. Case verdict FAIL because `check_log 'NOT-CHECKED'` missed. Every other check held, including **`check_stage_return_files` PASS**.

**Cost:** $6.94 billed, 14,273,091 attributed tokens. Soft ceilings: **within** 1900s, **within** $8.50, **within** 14,500,000 tokens. Attributed: main opus-5 $4.51 (31 tools), qa-engineer sonnet-5 $2.00 (34), backend-developer sonnet-5 $2.45 (51), database-developer sonnet-5 $1.56 (42). Tech-lead ran (read-only stage file on disk) but is not a billed attributed row in the printed ledger.

**Stage files (the Open item):** six files under `docs/delivery/tag/stages/` in the kept workdir, each with `STATUS` / `DID` / `VERIFIED` / `NOT-CHECKED` / `FLAGS` / `NEXT`:

- `database-developer.md`, `backend-developer.md`, `qa-engineer.md` — writers Wrote their own
- `tech-lead.md` — coordinator-persisted (read-only); first line says so
- `backend-developer-fixes.md`, `database-developer-fixes.md` — re-brief overwrites were supposed to reuse `<agent>.md`; the run used a `-fixes` suffix instead. Helper still matches `*/stages/*.md`. Do not add a filename ratchet from one timeout run.

Harvest: `docs/team/stack.md` and `docs/delivery/tag/log.md` both present. Board feed showed 4 distinct agents. `done when:` present on `$FULL_LOG`.

**Why NOT-CHECKED failed:** `$LOG` (`final_text()` / timeout fallback) is 322 bytes of mid-run board prose ("Waiting on qa-engineer and tech-lead"). It contains the English word "verified" (`what stages a and b verified`), so `check_log 'VERIFIED'` (`grep -qiE`) is a **false pass**. It does not contain `NOT-CHECKED`. `$FULL_LOG` ends mid-stage g (regression tests dispatched, re-verify queued). Same timeout-close shape as [run 9](2026-08-18-run-9.md), without that run's reconstructed close. Do not raise `EVAL_TIMEOUT` or `max_usd` in this release. Do not uncomment `check_subagent_log` (`$SUBAGENT_LOG` is 17,683 bytes this time — not empty — but still not the answer-key surface).

**Hash pin:** `coordinator_hash` is `d05a1c19db40bd66f7685eae3c7f93e87419eb6c3936762275eaf564becfee3d`, `as_of` 2026-08-20, waivers empty. The pin records that this billed run exercised the Interface + coordinator stage-return clauses. It does **not** claim 11/11.

**What this closes:** per-stage specialist returns are measurable on disk. [Run 10](2026-08-19-run-10.md)'s empty `$SUBAGENT_LOG` is no longer the only instrument.

**What this leaves open:** a headless `feature` run that hits 1200s still has no coordinator close on `$LOG`. That is not a new corpus Open row — it is the run-9 timeout, unchanged.
