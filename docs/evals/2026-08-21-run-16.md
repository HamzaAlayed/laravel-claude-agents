# Eval run 16 — close.md shape hook, billed `feature` — 2026-08-21

**Trigger:** [close.md shape-hook plan](../plans/2026-08-21-guild-v2-close-hook.md) Task 7. `enforce-close-file.sh` on `Write|Edit` already on `feat/close-md-hook`. This is the billed `feature` re-pin of `coordinator_hash` after that slice.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260821T144334Z`.

**Score:** **10/12 FAIL.** Harness printed `TIMED OUT after 1203s`. Two checks missed:

- `stage return missing VERIFIED: (tech-lead.md)` — coordinator-persisted file uses `VERIFIED (tech-lead):` (no `VERIFIED:` substring). Writers’ stage files have `VERIFIED:`.
- `close file missing VERIFIED: (tag/close.md)`

Harvest, delegation (4 agents), `done when:`, Tag artifacts, basename check, and both `$LOG` greps all PASS. No `-fixes`. Do not uncomment `check_subagent_log`.

**Cost:** No `result` line (timeout), so `billed.usd` is null. Attributed **$8.27** / 10,993,430 tokens — **within** 1900s (1203s), **within** 14,500,000 tokens. Attributed: main opus-5 $4.63 (30 tools, 5 Agent), backend-developer sonnet-5 $1.36 (37), database-developer sonnet-5 $0.80 (40), qa-engineer sonnet-5 $0.91 (15), tech-lead opus-4-8 $0.57 (read-only; not in the printed four-row ledger). Do not raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling.

**Close file (the 2.0 gate):** `docs/delivery/tag/close.md` exists (30 lines). It is a titled journal: `# Close file — feature: Tag (--api)`, then `STATUS: in-progress — stages a+b done…` (not `running|done|stopped`), `BOARD` with no colon, `VERIFIED (coordinator, independently):`. Last board still has stage f `▶ running`. The stub was not copied.

**Hook:** `enforce-close-file.sh` **was installed** in the eval workdir (`Write|Edit` in `.claude/settings.json`, script executable). The transcript has **zero** `Write`/`Edit` of `close.md`. Both close.md Writes were **Bash** `cat > docs/delivery/tag/close.md <<'EOF'` (main-thread, two times). The `Write|Edit` hook never saw the payload. Do not loosen the helper to accept `VERIFIED (`. Do not add a Bash matcher in this pin commit.

**What moved vs [run 15](2026-08-21-run-15.md):** `$LOG` VERIFIED / NOT-CHECKED PASS (run 15 missed both). Score 10/12 vs 9/12. Tokens back under 14.5M. tech-lead.md parenthetical again (run 15 had line-start `VERIFIED:`). Same timeout, same journal labels, new bypass (Bash heredoc).

**Hash pin:** `coordinator_hash` is `0e7794490fb97de5b9adb7a34f5934d0895da0d65b375fca1752eed1c3bdb20b`, `as_of` 2026-08-21, waivers empty. The pin records that this billed run exercised the shape-hook slice. It does **not** claim 12/12 or a 2.0.0 ship.

**What this leaves open:** a timed-out run still fails the close-file helper; the Write|Edit hook did not fire because close.md was written with Bash. VERSION stays **1.45.0**.
