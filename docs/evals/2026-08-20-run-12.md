# Eval run 12 — Guild 2.0 close file, billed `feature` — 2026-08-20

**Trigger:** [Guild v2 plan](../plans/2026-08-20-guild-v2.md) Task 8. Interface + coordinator `close.md` + spawn cap already on `feat/guild-v2`. This is the billed `feature` re-pin of `coordinator_hash` and the first live assertion of `check_delivery_close_file`.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260820T140140Z`.

**Score:** **10/12 FAIL.** Harness printed `TIMED OUT after 1202s`. Two file checks missed:

- `stage file basename is not a registered agent type (backend-developer-fixes)`
- `close file missing VERIFIED: (tag-api/close.md)`

Harvest, delegation (4 agents), `done when:`, tags artifacts, and `check_log` `VERIFIED` / `NOT-CHECKED` all PASS. Do not treat the log greps as a coordinator close — `$LOG` is 14,651 bytes of mid-run prose plus specialist returns; `grep -qiE` hits the words inside that stream.

**Cost:** No `result` line (timeout), so `billed.usd` is null. Attributed **$10.26** / 12,584,411 tokens — **above** the $8.50 soft ceiling (the harness did not print a dollar REGRESSED line because billed was empty). Wall clock **within** 1900s (1202s). Tokens **within** 14,500,000. Attributed: main opus-5 $5.32 (32 tools), backend-developer sonnet-5 $2.06 (73), qa-engineer sonnet-5 $1.26 (18), database-developer sonnet-5 $0.45 (24), tech-lead opus-4-8 $1.17 (read-only; not in the printed four-row ledger). Do not raise `max_usd` or `EVAL_TIMEOUT`.

**Close file (the 2.0 gate):** `docs/delivery/tag-api/close.md` exists. Last write is mid stage 3 (`STATUS: in-progress`, qa-engineer + tech-lead still `▶`). Labels are `VERIFIED (` and `NOT-CHECKED` — **no colons**. The helper requires `VERIFIED:` / `NOT-CHECKED:` / `STATUS:`. The board header in the run used `4 stages · cap: 6 spawns · done when:` then a re-plan to `5 stages · cap: 7 spawns`. Later stages (including the `-fixes` spawn) did not overwrite `close.md`.

**Stage files:** `database-developer.md`, `backend-developer.md`, `qa-engineer.md`, `tech-lead.md`, and **`backend-developer-fixes.md`**. Same `-fixes` suffix as [run 11](2026-08-20-run-11.md). v1.45.0 did not ratchet filenames; 2.0 does, and the helper correctly FAILed.

Harvest: `docs/team/stack.md` and `docs/delivery/tag-api/log.md` present.

**Hash pin:** `coordinator_hash` is `f475d97aac301b117fbc06b778c691bf8ca7d4a862b60fba27193be48d0e9765`, `as_of` 2026-08-20, waivers empty. The pin records that this billed run exercised the 2.0 Interface + coordinator close-file clauses. It does **not** claim 12/12 or a 2.0.0 ship.

**What this leaves open:** `close.md` is written, but not in the template shape and not after every stage. Re-brief still invents `-fixes`. That is why VERSION stays **1.45.0**. Do not uncomment `check_subagent_log`.
