# Eval run 18 — billed `feature-adaptive` pin — 2026-08-23

**Trigger:** [Adaptive plan](../plans/2026-08-23-guild-v2-adaptive.md) Task 8. [Design](../plans/2026-08-23-guild-v2-adaptive-design.md). Tasks 1–7 + harness gap fix already on `feat/adaptive-21`. This is the billed `feature-adaptive` pin of `coordinator_hash`.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature-adaptive
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api --adaptive`. Run id `20260823T110941Z`. `EVAL_JUDGE` not set.

**Score:** **12/17 FAIL.** Harness printed `TIMED OUT after 1204s`. Adaptive-specific rows missed:

- `check_adaptive_packet` — no `docs/delivery/*/packets/*-to-*.md` (found 0). `FROM:` / `TO:` greps FAIL with it.
- `check_adaptive_peer_router` — no `docs/delivery/*/stages/peer-router.md` (found 0).
- `check_adaptive_handoff` — no `handoff` on `$FULL_LOG` or `close.md`.

**`check_delivery_close_file` PASS.** Basename check PASS (`database-developer.md`, `backend-developer.md`, `qa-engineer.md`; no `-fixes`; no `tech-lead.md` — timeout before persist). Harvest PASS (`docs/team/stack.md`, `docs/delivery/tag/log.md`). Delegation (4 agents), `done when:`, Tag artifacts (migration, `Tag.php`, routes, feature tests), and both `$LOG` greps all PASS. Do not uncomment `check_subagent_log`.

**Cost:** No `result` line (timeout), so `billed.usd` is null. Attributed **$10.87** / 14,300,360 tokens — **within** 1900s (1204s), **within** 14,500,000 tokens (14,300,360). Attributed **REGRESSED** the $8.50 `max_usd` ceiling. Attributed: main opus-5 $5.87 (47 tools, 4 Agent), backend-developer sonnet-5 $1.45 (48), qa-engineer sonnet-5 $1.15 (16), tech-lead opus-4-8 $1.77 (24, read-only); database-developer sonnet-5 $0.63 (28; not in the printed four-row ledger). Do not raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling.

**Close file (the 2.0 gate, still PASS):** `docs/delivery/tag/close.md` exists. First four lines are helper shape: `VERIFIED:` / `NOT-CHECKED:` / `STATUS: running` / `BOARD:`. `VERIFIED:` starts the line — not `VERIFIED (`. Last board still has stage e `▶` and stage f unstarted (timeout). BOARD has no handoff line.

**Packet / peer-router:** `docs/delivery/tag/packets/` was created empty. Coordinator listed it after stages a, b, and d. `log.md` stage d: "No packets filed." qa-engineer return: "No packets filed — no defect found that needs a peer specialist." No `peer-router` Agent spawn (four launches only: database-developer, backend-developer, qa-engineer, tech-lead). Six-label `peer-router.md` missing.

**Hook:** `enforce-close-file.sh` **was installed** on Bash and Write|Edit. Transcript: first close.md attempt was Bash `cat > docs/delivery/tag/close.md <<'EOF'` — **blocked**. A later Write of a filled skeleton — **blocked** (shape). Seed succeeded as `cp` of helper-shape bytes, then Edit filled after the colons. Two later Edits that broke helper shape were **blocked**; the file on disk stayed labels-first. Do not loosen the helper. Do not treat `cp` as a 2.1 miss.

**What moved vs [run 17](2026-08-21-run-17.md):** harvest PASS (run 17 missed `stack.md` / `log.md`). Close file still PASS. New Adaptive rows all FAIL. Tokens 14.3M vs 8.7M (near the 14.5M ceiling). Attributed $10.87 vs $7.70. Same timeout class.

**Hash pin:** `coordinator_hash` is `e90994a23d464162fad73e03ef5c4bf73f3ab76788fbfe230c5de8a168704256`, `as_of` 2026-08-23, waivers empty. The pin records that this billed run exercised `--adaptive` and missed packet, `peer-router.md`, and the handoff line. Score is FAIL on those three; the 2.1.0 gate is Adaptive+close PASS, not 12/17.

**What this closes:** the billed Adaptive measurement. Inventory hash pin is current.

**What this leaves open:** Guild v2 Adaptive (2.1.0). VERSION stays **2.0.0**. Unreleased stays Unreleased. 2.2 graph still waits. Do not claim 2.1.0 shipped.
