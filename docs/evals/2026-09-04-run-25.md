# Eval run 25 — billed `feature-resume` pin — 2026-09-04

**Trigger:** user said **run it** after [run 24](2026-09-04-run-24.md). Same [2.3.0 plan](../plans/2026-09-04-guild-resume.md) Task 7. Helper now matches plugin-prefixed `cost.json` keys. Prompt copy unchanged.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature-resume
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260904T152154Z`. `EVAL_JUDGE` not set. Workdir kept at `/var/folders/65/fwmwydjj2ml5rwf5x45x6mc80000gn/T/laravel-agents-eval-feature-resume.XXXXXX.8yVzYaeiEq`.

**Score:** **13/14 FAIL.** Wall **1117s** — finished before the 1200s kill. Case verdict FAIL.

- `check_agent_absent database-developer` — **FAIL.** Honest. `cost.json` attributed agents include unprefixed `database-developer`: **39 turns**, **1,383,409** tokens, **$0.80**. `launched_without_measured_turns` is empty. Board close lists **two** database-developer stages (`a ✔ database-developer` and `a2 ✔ database-developer factory`).
- Close file, `$FULL_LOG` `VERIFIED`/`NOT-CHECKED`, Tag HTTP/tests, harvest, graph, `done when:`, `check_delegated` (4 distinct) — PASS as printed.

Resume fired: `$FULL_LOG` opens with `docs/delivery/tag/close.md` was `STATUS: running`, so this is a resume, not a fresh run. Skip still did not hold. The coordinator grew the board to **7 stages** against cap **6** (`remaining: 0`).

**Cost:** billed **$6.93** — **within** $8.50. Rate table **STALE**. Attributed **15,615,338** tokens — **REGRESSED** vs 14,500,000. Do **not** raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling. The skip miss is the ship gate, not the token line.

**Hash pin:** `coordinator_hash` is still `053a0599640b52ba6325c004879a22b5066a93a1e4f88121ff1ff42673f970f6` (prompt unchanged since run 24). `as_of` 2026-09-04, `waivers: []`. VERSION stays **2.2.2**. Unreleased stays Unreleased.

**What this closes:** the run-24 helper lie. Skip now fails when `database-developer` actually ran.

**What this leaves open:** Guild 2.3.0 resume. Prompt-layer skip did not stick on a second billed run. Do not uncomment `check_subagent_log`. Do not tag.
