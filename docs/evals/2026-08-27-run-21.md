# Eval run 21 — billed `feature` pin — 2026-08-27

**Trigger:** [2.2.0 plan](../plans/2026-08-24-guild-v2-adaptive-graph.md) Task 12 re-pin after run 20. Interface tightened (`6b83c31`): first Write of `graph.md` is a byte copy of the stub, then Edit only after the colons. This is the billed pin of `coordinator_hash` for default `graph.md`.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260827T110033Z`. `EVAL_JUDGE` not set. `feature-adaptive` not re-run: the 2.2.0 gate is default graph + close.

**Score:** **13/13 PASS.** Harness printed `TIMED OUT after 1203s`. Case verdict PASS — checks scored from disk.

- `check_delivery_graph_file` — **PASS.** `docs/delivery/tag/graph.md` is stub-shaped:

```
NODES: database-developer, backend-developer, qa-engineer, tech-lead
EDGES: database-developer -> backend-developer, backend-developer -> qa-engineer, backend-developer -> tech-lead
PARALLEL: qa-engineer || tech-lead
ON-FAIL: stop
```

Each `NODES:` token is a registered `agents/*.md`. No packets (Adaptive off) so hop-TO is skipped.

- `check_delivery_close_file` — **PASS.** Helper shape: `VERIFIED:` / `NOT-CHECKED:` / `STATUS: running` / `BOARD:`. `VERIFIED:` starts the line — not `VERIFIED (`.

Tag artifacts, feature test, delegation (4 agents), `done when:`, harvest, stage returns, `$LOG` `VERIFIED` / `NOT-CHECKED` — all PASS. Do not uncomment `check_subagent_log`.

**What moved vs [run 20](2026-08-27-run-20.md):** default graph was a prose briefing missing `PARALLEL:` / `ON-FAIL:`. After the byte-copy Interface sentence, the coordinator copied the four-line stub and filled after the colons.

**Cost:** no `result` line (timeout), so `billed.usd` is null. Attributed **$13.20** / **15,783,225** tokens — **within** 1900s (1203s), **REGRESSED** vs 14,500,000 tokens (15,783,225). Do not raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling. Attributed: main opus-5 $6.72 (51 tools); backend-developer sonnet-5 $1.25 (44); qa-engineer sonnet-5 $1.31 (22); database-developer sonnet-5 $0.75 (31).

**Close file (2.0 gate, still PASS):** `docs/delivery/tag/close.md` exists. Labels-first. Last board still has stage c `▶` (timeout during qa-engineer ‖ tech-lead). Do not loosen the helper.

**Hash pin:** `coordinator_hash` is `029de20983a52e63bd4d87e0ec8e3a07e7be7b02ed7c7283c0e460042721c148`, `as_of` 2026-08-27, waivers empty. The pin records that this billed run exercised default `graph.md` and **passed** graph labels + registered nodes + close. Score is 13/13; the 2.2.0 gate is graph+close PASS, not “finished before timeout”.

**What this closes:** Guild v2 graph (2.2.0). VERSION is **2.2.0**.

**What this leaves open:** Adaptive hop persist (`peer-router.md` / `handoff:`) still missed on run 20 — not this gate. Token ceiling still 14.5M. Do not tag until CI is green on the PR.
