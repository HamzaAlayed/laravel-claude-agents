# Eval run 14 — close.md procedure, billed `feature` — 2026-08-21

**Trigger:** [close.md procedure plan](../plans/2026-08-20-guild-v2-close-procedure.md) Task 6. Next-Write procedure + `VERIFIED (` ban + line-anchored helper already on `feat/close-md-procedure`. This is the billed `feature` re-pin of `coordinator_hash` after that slice.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260821T083630Z`.

**Score:** **9/12 FAIL.** The process **finished** (1046s, billed result line present). Three checks missed:

- `final answer carries NOT-CHECKED` — `$LOG` is 850 bytes of mid-board prose (“Both running…”). `check_log 'VERIFIED'` PASSes on `verified by me` in that board (false pass). `$FULL_LOG` does contain `**NOT-CHECKED:**`; that is not the answer-key surface.
- `stage return missing VERIFIED: (tech-lead.md)` — coordinator-persisted read-only file uses `VERIFIED (tech-lead's own read-only commands):`
- `close file missing VERIFIED: (tag/close.md)`

Harvest, delegation (4 agents), `done when:`, Tag artifacts, and basename check all PASS. No `-fixes`. Do not uncomment `check_subagent_log`.

**Cost:** **$6.33 billed** / 11,258,461 attributed tokens. Soft ceilings: **within** 1900s (1046s), **within** $8.50, **within** 14,500,000 tokens. Attributed: main opus-5 $5.12 (32 tools), backend-developer sonnet-5 $1.59 (27), database-developer sonnet-5 $0.95 (29), qa-engineer sonnet-5 $0.74 (11), tech-lead opus-4-8 $0.96 (read-only; not in the printed four-row ledger). Do not raise `max_usd` or `EVAL_TIMEOUT`.

**Close file (the 2.0 gate):** `docs/delivery/tag/close.md` exists and is a **final** write (`STATUS: done`, all 5 stages closed, spawn 4 of cap 6). Labels are still `VERIFIED (coordinator's own commands…):` and `NOT-CHECKED (coordinator):` — parenthesis before the colon, so `^VERIFIED:` misses. `STATUS: done` would have passed the vocab check. `BOARD:` is present. The next-Write procedure got a finished close file; the ban did not stick. Do not loosen the helper to accept `VERIFIED (`.

**What moved vs [run 13](2026-08-20-run-13.md):** no timeout; billed USD in ceiling; close.md overwritten through `STATUS: done`; spawn cap held (4 of 6). Same label paraphrase. tech-lead stage file now FAILs the six-label helper for the same parenthetical.

**Hash pin:** `coordinator_hash` is `33d55f54b7ef492304bef703546f611899901cfbb94e393cd560cca6a2510d47`, `as_of` 2026-08-21, waivers empty. The pin records that this billed run exercised the procedure slice. It does **not** claim 12/12 or a 2.0.0 ship.

**What this leaves open:** a completed run still fails the close-file helper on `VERIFIED (`. VERSION stays **1.45.0**.
