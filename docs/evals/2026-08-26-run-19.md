# Eval run 19 — billed `feature-adaptive` pin — 2026-08-26

**Trigger:** [2.1.1 plan](../plans/2026-08-24-guild-v2-adaptive-graph.md) Task 5. [Design](../plans/2026-08-24-guild-v2-adaptive-graph-design.md). Tasks 1–4 already on `feat/v2.1.1-adaptive-hop`. This is the billed `feature-adaptive` pin of `coordinator_hash`.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature-adaptive
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api --adaptive`. Run id `20260826T164151Z`. `EVAL_JUDGE` not set.

**Score:** **16/17 FAIL.** Harness printed `TIMED OUT after 1203s`. Adaptive-specific rows and close all PASS:

- `check_adaptive_packet` — PASS. Two packets: `docs/delivery/tag/packets/database-developer-to-backend-developer.md` and `backend-developer-to-qa-engineer.md`. Both carry `FROM:` / `TO:` / `SUMMARY:` / `PATHS:`.
- `check_adaptive_peer_router` — PASS. `docs/delivery/tag/stages/peer-router.md` exists with six labels.
- `check_adaptive_handoff` — PASS. Board line on `$FULL_LOG`: `handoff: database-developer → backend-developer`.
- `check_delivery_close_file` — PASS.

The one miss is `check_touched tests/` — `feature test added` FAIL. `qa-engineer` was never spawned (timeout after stage b). Harvest PASS (`docs/team/stack.md`, `docs/delivery/tag/log.md`). Delegation (3 agents: database-developer, peer-router, backend-developer), `done when:`, Tag artifacts (migration, `Tag.php`, routes), and both `$LOG` greps all PASS. Do not uncomment `check_subagent_log`. Do not require 17/17.

**Writer-filed, not coordinator fallback.** `log.md` stage a′: "database-developer wrote a packet unprompted." Pass 1 rejected on structure (`PATHS I OWN:` / missing `SUMMARY:`). Coordinator sent the author back via `SendMessage` — "no coordinator authorship of a writer's artifact." Pass 2 valid. Backend-developer then wrote `backend-developer-to-qa-engineer.md` (qa never launched). `KEEP_WORKDIR` packets/ matches that: two writer files, no coordinator-authored fallback.

**`peer-router` spawn: yes.** Three Agent launches: database-developer, peer-router, backend-developer. peer-router sonnet-5 $0.28 / 9 tool calls (Read + Bash only). FLAG: `agents/peer-router.md` still says "writer has named" — that did **not** block this run, because a writer *had* named a packet. It remains a concern for a coordinator-fallback-only run (the router description still requires a writer-named packet). Do not loosen the helper to paper over that.

**Cost:** No `result` line (timeout), so `billed.usd` is null. Attributed **$6.33** / 8,494,454 tokens — **within** 1900s (1203s), **within** $8.50, **within** 14,500,000 tokens (8,494,454). Attributed: main opus-5 $3.51 (Agent 3, SendMessage 2, Write 1); backend-developer sonnet-5 $1.23 (42); database-developer sonnet-5 $1.31 (41); peer-router sonnet-5 $0.28 (9). Do not raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling.

**Close file (the 2.0 gate, still PASS):** `docs/delivery/tag/close.md` exists. First four lines are helper shape: `VERIFIED:` / `NOT-CHECKED:` / `STATUS: running` / `BOARD:`. `VERIFIED:` starts the line — not `VERIFIED (`. Last board still has stage a `running` and stages b–d queued (timeout; later board lives on `$FULL_LOG`). BOARD on disk has no handoff line; handoff is on the live board. Seed used `cp` of helper-shape bytes (Write `content` vs hook `contents` again). Do not loosen the helper. Do not treat `cp` as a 2.1.1 miss.

**Basename:** `database-developer.md`, `backend-developer.md`, `peer-router.md`; no `-fixes`; no `tech-lead.md` / `qa-engineer.md` — timeout before those stages.

**What moved vs [run 18](2026-08-23-run-18.md):** Adaptive packet, peer-router.md, and handoff all PASS (run 18 missed all three). Close file still PASS. Harvest still PASS. Writer filed the hop; coordinator fallback was not needed. Tokens 8.5M vs 14.3M. Attributed $6.33 vs $10.87 (now inside $8.50). Same timeout class (1203s).

**Hash pin:** `coordinator_hash` is `e4c54674a0cd3c9499d96529f718366892b4d0cfc1915c47723a63f9deb43606`, `as_of` 2026-08-26, waivers empty. The pin records that this billed run exercised `--adaptive` and **passed** packet, `peer-router.md`, handoff, and close. Score is FAIL on the feature-test row only; the 2.1.1 gate is Adaptive+close PASS, not 16/17.

**What this closes:** the billed Adaptive required-hop measurement. Inventory hash pin is current. VERSION is **2.1.1**.

**What this leaves open:** Guild v2 graph (2.2.0). 2.2 still waits. Do not tag until CI is green on the PR. Do not start Phase B. Do not add `graph.md`.
