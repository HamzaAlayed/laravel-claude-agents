# Eval run 17 — close.md Bash write deny, billed `feature` — 2026-08-21

**Trigger:** [close.md Bash-write-deny plan](../plans/2026-08-21-guild-v2-close-bash.md) Task 6. `enforce-close-file.sh` on Bash already on `feat/close-md-bash`. This is the billed `feature` re-pin of `coordinator_hash` after that slice.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260821T164530Z`.

**Score:** **10/12 FAIL.** Harness printed `TIMED OUT after 1203s`. Two checks missed:

- `harvest persisted the stack snapshot (routing-table artifact)` — no `docs/team/stack.md`
- `harvest persisted the delivery log (routing-table artifact)` — no `docs/delivery/*/log.md`

**`check_delivery_close_file` PASS.** Basename check PASS (`database-developer.md`, `backend-developer.md`, `qa-engineer.md`, `tech-lead.md`; no `-fixes`). Delegation (4 agents), `done when:`, Tag artifacts, and both `$LOG` greps all PASS. Do not uncomment `check_subagent_log`.

**Cost:** No `result` line (timeout), so `billed.usd` is null. Attributed **$7.70** / 8,714,915 tokens — **within** 1900s (1203s), **within** 14,500,000 tokens. Attributed: main opus-5 $3.57 (28 tools, 5 Agent), backend-developer sonnet-5 $1.57 (44), database-developer sonnet-5 $0.98 (32), qa-engineer sonnet-5 $1.02 (20), tech-lead opus-4-8 $0.57 (read-only; not in the printed four-row ledger). Do not raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling.

**Close file (the 2.0 gate):** `docs/delivery/tag/close.md` exists. First four lines are helper shape: `VERIFIED:` / `NOT-CHECKED:` / `STATUS: running` / `BOARD:`. Extra `FLAGS:` after the board. Last board still has stage c `▶ running` (timeout). Stub was copied, then filled via Edit.

**Hook:** `enforce-close-file.sh` **was installed** on Bash and Write|Edit. Transcript: first close.md attempt was Bash `cat > docs/delivery/tag/close.md <<'EOF'` (journal, `VERIFIED (coordinator)`) — **blocked**. A later Write of a filled skeleton without copying the stub first — **blocked** (shape). Seed succeeded as Bash `cp …/skills/delivery-templates/close.md docs/delivery/tag/close.md` (not in this slice's deny list), then Edit filled after the colons and the Write|Edit hook allowed it. Helper labels on disk. Do not loosen the helper. Do not treat `cp` as a 2.0 miss.

**What moved vs [run 16](2026-08-21-run-16.md):** close file and `$LOG` labels PASS. Harvest FAILed (run 16 harvested; this run was still in stage c when killed). Score still 10/12. Tokens 8.7M vs 11.0M. Same timeout.

**Hash pin:** `coordinator_hash` is `80b5e9c069cf773e67790fafa7e3a601915c669d5872b20a05e38d9b367ff46a`, `as_of` 2026-08-21, waivers empty. The pin records that this billed run exercised the Bash-deny slice. Score is still FAIL on harvest; the 2.0 gate is close file PASS, not 12/12.

**What this closes:** Guild v2 Supervisor (2.0.0) — close file on disk in helper shape under timeout. VERSION **2.0.0**. 2.1 Adaptive and 2.2 graph wait.
