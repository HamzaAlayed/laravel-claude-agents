# Eval findings — run 6 (2026-08-04, post-1.31.1, sequential, judge on)

Run id `20260804T162215Z`. Sequential, `EVAL_TIMEOUT=1800`, `EVAL_JUDGE=1`.
First run with the cost instrument (v1.31.0/.1), the literature-gap tranche, and
per-agent `effort` all in place — and the first run ever with the rubric judge
exercised against real output.

## Verdict

**5/5 cases, 19/19 checks, and 5/5 judged PASS.** Matches run 5's quality on
checks and adds an independent judge agreeing with all five verdicts.

| case | verdict | checks | duration | billed | tokens | judge |
| --- | --- | --- | --- | --- | --- | --- |
| n-plus-one | PASS | 4/4 | 302s | $1.82 | 3,669,451 | PASS 5/5 |
| policy | PASS | 3/3 | 381s | $2.19 | 4,371,934 | PASS 5/5 |
| action | PASS | 4/4 | 1217s | $5.16 | 9,699,200 | PASS 4/5 |
| tests | PASS | 4/4 | 633s | $2.76 | 3,697,406 | PASS 4/5 |
| hygiene | PASS | 4/4 | 106s | $0.57 | 579,315 | PASS 5/5 |

**Total: $12.50 billed, ~44 min wall clock.** This is the first run in the
project's history with a cost figure attached, which was the point.

## The headline number, finally answered

Run 5 left one question: *why does `qa-engineer` burn ~138k tokens in a single
invocation?* Run 6 answers it — **tool-call volume**:

| agent | tokens | tool calls | billed share | model |
| --- | --- | --- | --- | --- |
| qa-engineer | 4,186,674 | **73** | $2.28 | claude-sonnet-5 |
| main | 3,572,080 | 29 | $3.99 | claude-opus-5 |
| tech-lead | 1,107,713 | 15 | $2.23 | claude-opus-4-8 |
| backend-developer | 832,733 | 24 | $0.52 | claude-sonnet-5 |

(`action` case.) `qa-engineer` makes **73 tool calls** — more than the other three
agents combined — and it is the largest lane by tokens despite running on Sonnet.
`tech-lead` is the opposite shape: a fifth of qa's tokens for nearly the same
dollars, because Opus 4.8 costs ~5× per token. **Time and money and tokens point
at three different targets**, which is exactly why three ceilings now exist.

Per-agent model attribution is correct per lane, which also settles the design
question recorded in the plan: the transcript *can* attribute spend per agent.

## Findings

### 1. `n-plus-one` is creeping badly — 96 → 157 → 178 → 302s

It breached its 250s ceiling. Four runs, and each one slower: **3.1× since run 3**
on a case that finds one N+1 in one Blade template and delegates nothing (single
opus-5 lane, 34 tool calls, $1.82). The ceiling is raised to 400s to keep it
carrying information, but the creep is the thing to explain, not absorb. Nothing
in 1.30.0/1.31.x targeted this case, so the likely driver is upstream: thinking is
on by default on Opus 5, and this case's prompt is a single read-only audit where
extra deliberation buys nothing. **Candidate lever for the next cycle:** the
`/audit-n-plus-one` command's own effort, now that per-agent `effort` is proven
to work.

### 2. The judge earned its keep on its first outing — and found a real answer-key hole

Two cases scored 4/5, and both criticisms are legitimate gaps the regex answer key
cannot see:

- **`action`** — *"app/Actions/ is untracked and its body is absent from the diff,
  so the mail fan-out, site_stats increment and Log::info re-implementations are
  asserted by the transcript, not shown."* This is a genuine defect in the answer
  key, not in the run: `checks_action` greps `git diff`, and **`git diff` does not
  show untracked files**. A case whose whole job is *creating a new Action class*
  therefore verifies the new file's existence but never its contents. Fixable with
  `git add -N` (intent-to-add) before diffing, or by reading the file directly.
- **`tests`** — *"Authz assertion exists but is `->todo()`, so it never executes —
  the IDOR is documented rather than demonstrated, and CI stays green with the
  flaw live."* This one is aimed at a **deliberate pack decision**: v1.24.0 told
  `qa-engineer` to assert the secure outcome marked `->todo()` precisely so it
  documents the flaw without freezing it into a passing test. The judge is right
  that a `->todo()` test protects nothing today. Whether that trade is correct is
  a real question for the human — do not "fix" it by deleting the v1.24.0 rule
  without deciding.

The judge stayed advisory throughout and changed no verdict, as designed.

### 3. The cost instrument had two defects that only real data could expose

Both found by this run, both fixed:

- **Dated model ids were unpriced.** Transcripts report
  `claude-haiku-4-5-20251001`; the rate table and agent frontmatter use the alias
  `claude-haiku-4-5`. The dated id missed the table, fell through to the default
  Opus rate, and priced `scrum-master` **5× too high**. The rate-table
  reconciliation caught it rather than reporting a wrong number quietly: billed
  $0.622 against a repriced floor of $0.690, with an implied cache-write
  multiplier of 0.99 — below the 1.25 tier floor, i.e. arithmetically impossible.
  Fixed by resolving a trailing `-YYYYMMDD`, plus a test asserting every model any
  agent pins is priceable.
- **A phantom `unknown-agent` lane.** Three cases spawned no subagent at all
  (empty board feed) yet reported an `unknown-agent`, which the harness announced
  as *"launched but unmeasured (async?)"* — inventing a lane in the one report
  meant to be authoritative about lanes. Cause: any `task_started` line carrying a
  `tool_use_id` was treated as an agent launch, with a fallback name. A
  `task_started` without a `subagent_type` is not an agent launch and is now
  skipped.

### 4. Token totals are >99% cache reads, so tokens alone cannot ratchet cost

Every case: n-plus-one 3,666,860 cache of 3,669,451 total; the others 100.0% to
one decimal. Cache reads bill at 0.1× input, so **`max_tokens` measures context
volume × turns, not spend.** A token-only ceiling would have been close to
useless as a cost guard — it would move with prompt size and turn count while
missing a model-mix regression entirely (a sonnet → opus re-tier keeps tokens flat
and triples the bill).

`baseline.json` therefore now carries **three** ceilings — `max_seconds`,
`max_tokens`, `max_usd` — with the division of labour written down: dollars speak
for cost, tokens for work volume, and the rate-table reconciliation distinguishes
a price change from a regression.

### 5. `policy` and `action` are bimodal, and run 6 swapped their modes

Run 5 delegated `policy` (994s) and fast-pathed `action` (420s). Run 6 did the
exact opposite: `policy` finished in **381s with no delegation at all**, `action`
took **1217s across four agents**. Both breached or undershot ceilings set from
the other mode.

This is the coordinator's fast path working as designed, but it makes those two
cases' ceilings mode-dependent. Ceilings are seeded from run 6 **without**
inflating them to cover both modes, and `baseline.json` records the caveat: on a
REGRESSED reading for either case, check the agent list in `<case>.cost.json` for
delegation before calling it a regression.

### 6. The tranche's over-trigger risk did not materialise — but was barely tested

The held tranche's item 1 (`NOT-CHECKED` escalation) carried a stated risk: a
specialist dumping adjacent surfaces into `NOT-CHECKED` could stall a lane on
checkpoint prompts. **No case produced a checkpoint prompt or a stalled lane**,
and no case exceeded its stage budget.

That is a weak negative, though: only `action` and `tests` delegated at all, and
neither hit a checkpoint category. `policy` — the case most likely to trigger one,
and the case that delegated in run 5 — did not delegate this time. **The
escalation path is still effectively unexercised.** Do not read run 6 as
clearing it.

## Baseline reseed

All three ceilings seeded for all five cases (previously `max_tokens` was `null`
by design and `max_usd` did not exist). See `tests/eval/baseline.json` for the
per-case basis strings. Changes worth noting:

- `n-plus-one` 250s → **400s** (finding 1; the creep is recorded, not hidden).
- `action` 600s → **1400s** (finding 5; run 5's basis note predicted this exact
  breach and called it the intended signal).
- `policy` **unchanged at 1100s** despite finishing in 381s — lowering it to run
  6's undelegated figure would make every delegating run read REGRESSED.
- `tests` and `hygiene` unchanged; both were within.

## Run-5 checklist, closed

- [x] Finding 1 (polluted feeds) fixed before this run — feeds were clean, and
      three cases correctly showed *no* agent events rather than inherited debris.
- [x] `EVAL_JUDGE=1` exercised for the first time. It works, stayed advisory, and
      found two real answer-key gaps (finding 2).
- [x] The tranche landed before this run (v1.31.0) and was watched. See finding 6
      — no over-triggering, but weakly tested.
- [x] README's worktree claim left alone, as instructed.
- [ ] **Still open: is `policy`'s security-engineer stage intended cost?** Run 6
      cannot answer it — `policy` did not delegate this time, so there was no
      security-engineer stage to measure. Carried to run 7.

## Run-7 checklist

- [ ] Explain `n-plus-one`'s 3.1× creep (finding 1). Try lowering effort on the
      read-only audit path now that per-agent `effort` is proven.
- [ ] Fix `checks_action` so it can see untracked files (`git add -N` before
      diffing). The judge found this; the regex key cannot.
- [ ] Decide the `->todo()` authz question (finding 2) — it is a live trade-off in
      the pack, not a bug to quietly patch.
- [ ] Force `policy` to delegate at least once, so the `NOT-CHECKED` escalation
      path and the security-engineer stage cost both get a real measurement
      (findings 5 and 6, plus the still-open run-5 item).
- [ ] Re-check `max_usd` against published prices before trusting a breach —
      Sonnet 5's introductory rate expires 2026-08-31, and three of the five
      cases run Sonnet lanes.
