# Eval run 26 — billed `feature-resume` pin — 2026-09-04

**Trigger:** skip carve-out after [run 25](2026-09-04-run-25.md) (`Do not Agent a skipped writer`). Same [2.3.0 plan](../plans/2026-09-04-guild-resume.md) Task 7 gate. [Design](../plans/2026-09-04-guild-resume-design.md).

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature-resume
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260904T155031Z`. `EVAL_JUDGE` not set. Workdir kept at `/var/folders/65/fwmwydjj2ml5rwf5x45x6mc80000gn/T/laravel-agents-eval-feature-resume.XXXXXX.5JPx1wWmkY`.

**Score:** **14/14 PASS.** Wall **703s**. Case verdict PASS.

- `check_agent_absent database-developer` — **PASS.** Honest. `cost.json` attributed agents are `main`, `laravel-team:backend-developer`, `laravel-team:qa-engineer`, `laravel-team:tech-lead`. No `database-developer` key and none with that last `:` segment. `launched_without_measured_turns` is empty. `check_delegated` saw **3** distinct (backend, qa, tech-lead).
- Close file, `$FULL_LOG` `VERIFIED`/`NOT-CHECKED`, Tag HTTP/tests, harvest, graph, `done when:` — PASS.

Resume fired and skip held. `$FULL_LOG` opens with stage a `✔ skipped-as-done`. `close.md` `BOARD` says `a ✔ database-developer — Tag model + tags migration (skipped as done on resume)`. Factory/`HasFactory` (not named in planted `DID:`) went to qa-engineer, not a second database-developer stage. Cap unused 3 of 6.

**Cost:** billed **$4.32** — **within** $8.50. Rate table **STALE**. Attributed **9,071,302** tokens — **within** 14,500,000. Do not raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling.

**Hash pin:** `coordinator_hash` is `b5fd40f2f1a1b91c9c3af90694545bf94ef048c8bc05c971f5df869f8f1c7663`, `as_of` 2026-09-04, waivers empty. The pin records that this billed run skipped the completed database-developer stage.

**What this closes:** Guild 2.3.0 resume. VERSION **2.3.0**.

**What this leaves open:** Token ceiling still 14.5M. Rate table STALE. Do not tag until CI is green on the PR.
