# Eval run 24 — billed `feature-resume` pin — 2026-09-04

**Trigger:** [2.3.0 plan](../plans/2026-09-04-guild-resume.md) Task 7. [Design](../plans/2026-09-04-guild-resume-design.md). Tasks 1–6 already on `feat/v2.3.0-resume`. This is the billed `feature-resume` pin of `coordinator_hash` after resume-from-running-close.md.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature-resume
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260904T145248Z`. `EVAL_JUDGE` not set. Workdir kept at `/var/folders/65/fwmwydjj2ml5rwf5x45x6mc80000gn/T/laravel-agents-eval-feature-resume.XXXXXX.pkOL0CuKc1`.

**Score:** harness printed **14/14 PASS**, wall **843s**. That skip row is a **false PASS**. Do not ship 2.3.0. VERSION stays **2.2.2**.

- `check_delivery_close_file` — **PASS.** `docs/delivery/tag/close.md` is helper shape: `VERIFIED:` / `NOT-CHECKED:` / `STATUS: done` / `BOARD:`. `VERIFIED:` starts the line — not `VERIFIED (`.
- `$LOG` `VERIFIED` / `NOT-CHECKED` — scored via `check_log_anywhere` on `$FULL_LOG`. `$LOG` is a mid-run waiting turn (c ‖ d still `▶`). `$FULL_LOG` has the four labels, including an opening resume close (`STATUS: running`).
- Tag HTTP/tests, harvest, graph, `done when:`, delegation (`check_delegated` saw **4** distinct) — PASS as printed.

**Skip gate — FAIL (helper lied).** `check_agent_absent database-developer` printed PASS because it looked for the exact key `database-developer`. `feature-resume.cost.json` attributed agents as plugin-prefixed names. `laravel-team:database-developer` is present: **6 turns**, **194,331** tokens, **$0.29**, tools Bash/Edit/Read/Write. `launched_without_measured_turns` is empty. The harness attributed printer shows only the top 4 agents by tokens, so this fifth lane did not appear on the console summary (main / backend-developer / qa-engineer / tech-lead). The board feed lists `database-developer` as one of four distinct agents.

The coordinator **did** resume: `$FULL_LOG` opens with `Resuming: prior run left STATUS: running`. It then re-Agented database-developer because “the database stage is missing the factory.” Planted `DID:` named `Tag.php` and the tags migration, both still on disk. Missing `TagFactory` is not a skip-breaker under the spec (writer skip = helper-shaped `STATUS: done` **and** a `DID:`/`VERIFIED:` path still exists). Stage file was overwritten (`STATUS: DONE`, HasFactory + TagFactory). Main-thread `Agent` count is **5** (database-developer, backend-developer, qa-engineer, tech-lead, then backend-developer again for a 201 fix).

**Cost:** billed **$4.87** — **within** $8.50. Rate table **STALE** (`agrees: false`; implied cache-write multiplier 1.21). Attributed **7,286,100** tokens — **within** 14,500,000. Do not raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling.

**Hash pin:** `coordinator_hash` is `053a0599640b52ba6325c004879a22b5066a93a1e4f88121ff1ff42673f970f6`, `as_of` 2026-09-04, waivers empty. The pin records that this billed run exercised resume copy. Skip did **not** hold. Score 14/14 is not a 2.3.0 ship gate.

**What this closes:** nothing product-side. Inventory hash pin is current so CI is not blocked on the unpinned hash.

**What this leaves open:** Guild 2.3.0 resume. Next billed `feature-resume` must fail `check_agent_absent` unless database-developer is truly absent, including `laravel-team:`-prefixed keys. Do not uncomment `check_subagent_log`. Do not tag.
