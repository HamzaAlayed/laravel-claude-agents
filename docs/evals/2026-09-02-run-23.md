# Eval run 23 — billed `feature` pin — 2026-09-02

**Trigger:** [2.2.2 plan](../plans/2026-09-02-guild-v2-close-print.md) Task 7. [Design](../plans/2026-09-02-guild-v2-close-print-design.md). Tasks 1–6 already on `feat/v2.2.2-close-print`. This is the billed `feature` pin of `coordinator_hash` after close print on every board.

**Command:**

```sh
KEEP_TRANSCRIPT=1 KEEP_WORKDIR=1 ./tests/eval/run-evals.sh feature
```

Default `EVAL_TIMEOUT=1200`. `max_usd` left at $8.50. Prompt `/make-feature Tag --api`. Run id `20260902T204907Z`. `EVAL_JUDGE` not set. `feature-adaptive` not re-run: the 2.2.2 gate is default close print + `$LOG`/`$FULL_LOG` `VERIFIED` / `NOT-CHECKED`.

**Score:** **13/13 PASS.** Wall **1114s** — finished before the 1200s kill. Case verdict PASS.

- `check_delivery_close_file` — **PASS.** `docs/delivery/tag/close.md` is helper shape: `VERIFIED:` / `NOT-CHECKED:` / `STATUS: done` / `BOARD:`. `VERIFIED:` starts the line — not `VERIFIED (`.
- `$LOG` `VERIFIED` / `NOT-CHECKED` — **PASS.** Closing answer opens with the four labels (`STATUS: done`). The old last-turn `check_log` would have passed this run; `check_log_anywhere` was not needed to rescue a “waiting…” kill.
- `$FULL_LOG` also carries a **mid-run** close print (`STATUS: running`, `BOARD: … d ▶ · e ▶`) before the final `STATUS: done` block. That is the 2.2.2 stickiness: print after every `close.md` Write, not only the last turn.

Tag artifacts, feature test, delegation (4 agents: database-developer, backend-developer, qa-engineer, tech-lead), `done when:`, harvest (`docs/team/stack.md`, `docs/delivery/tag/log.md`), stage returns, graph — all PASS. Do not uncomment `check_subagent_log`.

**Graph:** `docs/delivery/tag/graph.md` is stub-shaped (`NODES:` / `EDGES:` / `PARALLEL:` / `ON-FAIL:`). `NODES:` tokens are registered. `PARALLEL:` used `qa-engineer + tech-lead` (label present; helper does not require `||`). No packets (Adaptive off).

**Cost:** billed **$5.37** — **within** $8.50. Rate table **STALE** (`agrees: false`; implied cache-write multiplier 0.70). Attributed **10,093,209** tokens — **within** 14,500,000. Attributed: main opus-5 $3.55 (62 turns, 4 Agent); backend-developer sonnet-5 $1.30 (53); database-developer sonnet-5 $1.64 (44); qa-engineer sonnet-5 $0.61 (20); tech-lead opus-4-8 $0.46 (13). Do not raise `max_usd`, `EVAL_TIMEOUT`, or the token ceiling.

**What moved vs [run 21](2026-08-27-run-21.md):** run 21 timed out at 1203s with `STATUS: running` and qa-engineer still `▶`. Run 23 finished in 1114s with `STATUS: done` on both `$LOG` and `close.md`, and `$FULL_LOG` shows an earlier printed close while d ‖ e were still running.

**Hash pin:** `coordinator_hash` is `d6df9f8882871d793c269bc9d5e7d284b8f0d20a4df7e1a766bbd6e14e402dcf`, `as_of` 2026-09-02, waivers empty. The pin records that this billed run exercised close print and **passed** close file + `$LOG` `VERIFIED` / `NOT-CHECKED`. Score is 13/13.

**What this closes:** Guild v2 close print (2.2.2). Inventory hash pin is current. VERSION is **2.2.2**.

**What this leaves open:** Token ceiling still 14.5M. Rate table STALE. Do not tag until CI is green on the PR.
